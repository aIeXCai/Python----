# AI 混合题库 Step 2 验收报告

报告日期：2026-09-11
对应 DEV：`docs/dev/2026-09-11-ai-mixed-question-bank-quiz-dev.md`
实施范围：Step 2（编程题归类与存量兼容）

## 1. 已完成代码

- `Problem` 管理响应增加所属小节、大单元、单元年级、可组卷状态和稳定原因码。
- 编辑服务支持设置/清空 `unit`；设置小节后服务端强制同步 `grade_tag`。
- 拒绝大单元、已归档小节、跨教师年级和单元/年级冲突。
- 题库 API 支持按年级、大单元、小节、未归类、关键词和可组卷状态筛选。
- 可组卷规则已固定：题目未归类、题目/单元已归档、题干为空或没有测试点时不可组卷。
- `sync_from_disk()` 的定向测试确认不覆盖人工 `unit` 和 `grade_tag`。
- 教师端“题库管理”已更名为“编程题库”，增加小节/可组卷筛选、卡片状态和编辑弹窗小节选择。

## 2. 验证

- Step 2 后端定向测试：6 项通过。
- AI 课程 + Execution 全量回归：214 项通过，2 项跳过。
- 教师编程题页与编辑弹窗定向测试：28 项通过。
- 前端全量：290 项通过，2 项跳过。
- ESLint：0 错误、32 个既有警告；Vite 生产构建通过。
- Django system check 和 migration check 通过。
- 本地真实 API 链路已验证：登录→读取未归类状态→关联小节→年级自动同步→按小节筛选→清空关联。验收临时数据已全部清理。

## 3. 存量数据状态

- AI 编程题：9 道，均保持原 `problem_id`、八年级标签和历史内容。
- Submission：9 条，未改动。
- AI 单元：0；已归类编程题：0。

## 4. 存量题归类决策

- 用户已确认现有 9 道编程题暂不归类。
- 不创建猜测性的单元、fixture 或题目映射。
- 9 道题保留未归类状态和既有八年级标签，历史练习链路不受影响。
- 后续可在教师端根据实际教学安排逐题关联小节。

该决策满足存量兼容与“不根据标题猜测”的约束，Step 2 验收完成，不阻塞 Step 3。
