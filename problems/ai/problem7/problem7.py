'''
注意事项：仅需填写标有①②③④⑤的部分，其他代码请勿修改。
提示：
- 使用 `while True` 开启循环。
- 使用 `input()` 获取输入。
- 使用 `if` 判断是否是 -1，如果是则 `break`。
- 使用 `if` 判断是否是偶数 (`n % 2 == 0`)。
- 使用变量 `total` 来记录总和。
'''

# 定义结果变量total用于存储偶数和
total = 0

# 使用无限循环不断读取输入
while ①:
    # 每次循环，读取一个输入，并转换为整数(int)
    n = ②(input())
    
    # 如果检测到输入是-1，则结束循环
    if n == -1:
        ③
    
    # 如果检测到输入是偶数，则累加到总和中
    if ④:
        total = ⑤

# 输出最终的偶数和结果
print(total)