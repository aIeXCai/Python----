'''
注意事项：仅需填写标有①②③④⑤的部分，其他代码请勿修改。
提示：
- 使用 input() 读取输入，并转换为整数。
- 外层循环控制行数（1 到 n）。
- 内层循环控制每行打印的 * 数量。
- 使用 end='' 取消换行，使 * 在同一行输出。
'''

# 读取行数 n
n = int(input())

# 外层循环：控制行数
for i in range(1, ①):
    # 内层循环：控制每行的 * 数量
    for j in range(1, ②):
        # 打印 *，不换行
        print('*', end=③)
    # 每行结束后换行
    ④