# 项目开发规范

## 新功能开发流程（必须遵循）

所有新功能开发必须走以下闭环，不可跳过任何步骤：

1. **PRD（产品需求文档）**
   - 写入 `docs/prd/YYYY-MM-DD-功能名-prd.md`
   - 内容：背景与问题、目标、用户故事、资源约束、功能边界、UI/UX规格、非功能需求、成功指标
   - 提交给用户确认后方可进入下一步

2. **DEV 文档（实施路线图）**
   - 写入 `docs/dev/YYYY-MM-DD-功能名-dev.md`
   - 内容：技术架构设计、实施步骤分解、关键文件清单、验证方案
   - 提交给用户确认后方可进入下一步

3. **逐步实施**
   - 按 DEV 文档中的步骤逐一执行
   - 每步完成后确认通过再进行下一步
   - 每完成一个 Step，在 DEV 文档对应标题后标注 ✅ 已完成

4. **测试验证**
   - 后端：curl/手动测试端点
   - 前端：运行 vitest 测试用例
   - 端到端验证完整功能链路
